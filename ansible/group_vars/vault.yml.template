# Copy to vault.yml and fill in real values. vault.yml is gitignored;
# keep it plaintext (qr practice) or encrypt with ansible-vault if preferred.
#
#   cp group_vars/vault.yml.template group_vars/vault.yml

vault_postgres_password: "CHANGE_ME_strong_random_password"
vault_jwt_secret: "CHANGE_ME_random_hex_at_least_32_chars"
vault_ai_api_key: "CHANGE_ME_zai_api_key"

# SMTP (Brevo)
vault_smtp_host: "CHANGE_ME_smtp_host"
vault_smtp_user: "CHANGE_ME_smtp_user"
vault_smtp_password: "CHANGE_ME_smtp_password"
